# SILVER 에이전트 작업 헌장

이 저장소에서 작업하는 모든 사람과 에이전트는 아래 규칙을 최우선으로 따른다. 이 문서는 기능 요구사항보다 먼저 읽어야 하는 디자인 보호 계약이다.

## 1. 최우선 원칙: 현재 디자인은 고정 자산이다

- 작업 시작 시점의 `index.html` 렌더링 결과와 `assets/**`가 유일한 시각적 기준이다.
- 기능 개발은 기존 디자인 위에 연결한다. 기능 구현을 이유로 디자인을 재구성하지 않는다.
- 기존 화면의 에셋, 동영상, 애니메이션, 로고, 아이콘, 이미지, 제목, 설명, 버튼 문구, 콘텐츠 순서, 여백, 크기, 색상, 폰트, 라운드, 그림자, 반응형 동작을 바꾸지 않는다.
- 디자인 변경이 기능 구현에 꼭 필요해 보이면 임의로 타협하지 말고 작업을 멈춘 뒤 변경 이유와 영향을 보고한다.
- 사용자가 명시적으로 디자인 변경을 요청한 경우에만 해당 범위 안에서 최소한으로 수정한다. 요청받지 않은 주변 디자인은 그대로 둔다.
- plushome 병합 이후의 본 화면 19개(0~18)와 입력 8단계, 결과 요약·상세 화면은 확정된 기준선이다. 단계 수, 화면 순서, 결과 섹션 순서와 고정 영역을 기능 편의상 바꾸지 않는다. `#start`와 `#ai-consulting`은 본 화면 번호 밖의 오버레이다.
- 서비스의 최초 진입 화면은 언제나 `#start` 스플래시다. 루트 주소, 빈 해시, 알 수 없는 해시를 홈으로 보내지 않는다.

## 2. 작업 전 필수 읽기

아래 문서를 모두 읽은 뒤 코드를 수정한다.

1. `AGENTS.md`
2. `docs/디자인_보호_가이드.md`
3. `docs/디자인_시스템_명세.md`
4. 담당 기능과 관련된 기존 문서

외부 에이전트에게 일을 전달할 때는 `docs/외부_에이전트_작업_프롬프트.md`를 사용한다.

## 3. 절대 금지 사항

- `index.html` 전체 덮어쓰기, 대량 재정렬, 전체 포맷팅, minify, 자동 변환
- React, Vue, Next.js 등 다른 프레임워크로의 임의 마이그레이션
- 전역 CSS reset, normalize, UI 프레임워크, 외부 컴포넌트 라이브러리 도입
- 기존 클래스명, `data-*`, `id`, 해시 라우트, DOM 계층, 화면 번호 변경 또는 삭제
- 기존 CSS 규칙의 삭제, 병합, 이름 변경, 우선순위 재구성
- 디자인 토큰의 값이나 의미 변경, 특히 현재 브랜드 색을 가리키는 `--blue` 별칭의 이름 변경
- 에셋 파일의 삭제, 이름 변경, 이동, 교체, 재인코딩, 압축, 크롭, 색상 보정
- 동영상을 이미지로 대체하거나 `autoplay`, `muted`, `loop`, `playsinline` 등의 재생 속성을 임의 변경
- 제목, 본문, 버튼명, 메뉴명, 대체 텍스트를 기능 개발자의 판단으로 수정
- 기존 화면을 `innerHTML`로 통째로 교체하거나 다시 생성
- 숨겨진 원본 `radio`·`select`와 이를 바텀시트로 연결하는 `.select-trigger` 구조를 제거하거나 브라우저 기본 선택 UI로 되돌리는 행위
- 입력 단계의 현재 조합을 다시 분리하거나 합치는 행위
- 결과 화면의 상단 `홈으로`, 현금흐름 요약, AI 추천, 전체 결과 5개, FAQ, 하단 공유·상담 CTA를 삭제·재배치하는 행위
- 최초 진입을 `#home`으로 바꾸거나 스플래시를 건너뛰는 자동 리다이렉트·로그인 복원·상태 복원
- 작업과 무관한 파일 수정, 사용자 변경 되돌리기, `git reset --hard`, `git checkout --` 사용
- 파일 전체에 자동 포매터를 실행해 불필요한 diff를 만드는 행위

## 4. 허용되는 기능 연결 방식

- 기존 요소를 그대로 사용하고 이벤트 리스너, API 어댑터, 상태 관리, 유효성 검사만 연결한다.
- 기존 `data-*`, `id`, 클래스 선택자를 우선 사용한다.
- 새 연결 지점이 필요할 때는 시각적 영향을 주지 않는 `data-*` 속성을 최소한으로 추가한다.
- 동적 값은 기존 요소의 `textContent`, `value`, `hidden`, `aria-*`를 통해 주입한다.
- 입력값의 원본 상태는 기존 `radio`와 `select`를 유지하고, 바텀시트 트리거에는 기존 동기화 함수를 통해 반영한다.
- 결과 데이터는 현재 결과 노드에 주입한다. 결과 화면 전체를 템플릿 문자열로 다시 만들지 않는다.
- 로딩, 오류, 빈 상태는 먼저 현재 구현된 화면 패턴과 클래스를 재사용한다.
- 새 로직은 작은 함수나 별도 모듈로 격리하고 기존 마크업 수정량을 최소화한다.
- 접근성 속성은 시각적 결과와 기존 동작을 바꾸지 않는 범위에서 추가할 수 있다.

## 5. 신규 기능과 신규 페이지

- 신규 페이지는 기존 화면을 재설계하지 않고 현재의 상단 바, 콘텐츠 여백, 카드, 입력 필드, 하단 고정 버튼, 하단 탭, 바텀시트 패턴을 조합해 만든다.
- 색상, 폰트 크기, 라운드, 그림자, 아이콘 형태를 새로 발명하지 않는다.
- 새 컴포넌트가 필요한 경우 가장 가까운 기존 컴포넌트를 복제해 토큰과 밀도를 유지한다.
- 기존 라우트의 이름, 순서, 동작을 보존한다. 새 라우트는 충돌 없이 추가한다.
- 일부 해시 이름은 현재 화면 내용과 역사적으로 일치하지 않는다. 이를 정리한다는 이유로 이름이나 번호를 변경하지 않는다. 정확한 매핑은 디자인 보호 가이드를 따른다.
- 신규 인증·딥링크·상태 복원 기능도 기본 진입 계약을 변경할 수 없다. 명시적으로 유효한 기존 해시로 들어온 경우만 해당 화면을 열고, 기본 진입은 `#start`를 유지한다.
- 새 시각 규칙이 정말 필요하면 구현 전에 제안서 형태로 색상, 크기, 사용 위치, 기존 패턴으로 해결할 수 없는 이유를 보고하고 승인을 기다린다.

## 6. 동시 작업 보호

- 이 저장소에는 다른 에이전트와 사용자의 미완료 변경이 있을 수 있다.
- 작업 직전에 `git status --short`와 관련 파일의 최신 내용을 다시 확인한다.
- 원격 `main`이 아니라 조정자가 지정한 정확한 브랜치·커밋과 작업 시작 시점의 화면을 기준으로 삼는다.
- 다른 변경과 같은 코드 구간이 겹치면 덮어쓰지 말고 충돌 범위를 보고한다.
- 사용자 또는 다른 에이전트가 만든 미추적 파일을 삭제하거나 정리하지 않는다.
- 작은 패치 단위로 수정하며, 매 패치 후 의도하지 않은 시각 변경이 없는지 확인한다.

## 7. 필수 작업 순서

1. 위 필수 문서를 읽는다.
2. `git status --short`로 기존 변경을 기록한다.
3. 담당 기능이 사용하는 현재 DOM, 선택자, 라우트, 에셋을 읽는다.
4. 홈, 담당 입력 화면, `#result`와 연결된 상세 화면을 320px, 375px, 390px, 500px에서 캡처하거나 눈으로 확인해 기준선을 확보한다.
5. 디자인을 건드리지 않는 연결 지점을 정한다.
6. 로직을 최소 패치로 구현한다.
7. 전체 파일이 아니라 변경한 줄 중심으로 diff를 확인한다.
8. 기존 라우트와 신규 기능을 검증한다.
9. 같은 네 가지 너비에서 전후 화면을 비교한다.
10. 최종 보고서에 기능 변경, 건드린 파일, 디자인 diff 유무, 검증 결과를 적는다.

## 8. 완료 기준

아래 조건을 모두 만족해야 완료다.

- 요청한 로직이 동작한다.
- 기존 화면의 픽셀 배치, 콘텐츠, 에셋, 애니메이션, 반응형 동작이 유지된다.
- 기존 해시 라우트가 모두 열린다.
- 루트 주소와 빈·알 수 없는 해시는 `#start` 스플래시를 표시하고, 스플래시의 시작 버튼을 눌러야 홈으로 이동한다.
- 입력 선택 트리거와 바텀시트가 원본 필드 값과 동기화된다.
- 5초 분석 화면 이후 결과 요약으로 이동하며 영상의 비율과 재생이 유지된다.
- 결과의 `홈으로`, 상세 보기, FAQ, 하단 공유·상담 버튼이 기존 위치에서 동작한다.
- 콘솔 오류와 깨진 에셋 요청이 없다.
- 키보드 포커스와 버튼 동작이 기존보다 나빠지지 않는다.
- 작업 범위 밖의 diff가 없다.
- 디자인 변경이 1px 또는 한 글자라도 발생했다면 요청 근거와 검증 결과가 명시되어 있다.

## 9. 보호 대상

특히 다음 항목은 명시적인 디자인 요청 없이는 수정하지 않는다.

- `index.html`의 HTML 구조, 인라인 CSS, 화면 문구 및 기존 동작
- `assets/design-system/**`
- `assets/icons/**`
- `assets/content/**`
- `assets/*.png`, `assets/*.mp4`, `assets/*.json`, `assets/lottie.min.js`, `assets/beach-data.js`
- 로고, 인트로, 홈 인사 애니메이션, 시작 카드, 분석 애니메이션, 뉴스·콘텐츠 이미지

세부 기준은 `docs/디자인_보호_가이드.md`와 `docs/디자인_시스템_명세.md`를 따른다.
