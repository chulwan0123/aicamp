{
  "name": "silver-parent-retirement",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "SILVER 부모님 노후 준비 서비스",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check api/advise.js && node --check api/share.js && node --check api/price.js && node --check js/address.js && node --check js/property.js && node --check js/silver-engine.js && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
